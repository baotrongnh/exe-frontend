# PLAN151025

## Task List

1. #USER01
   - Navbar component already have a button "Become Employer", you should to:
     1. If logged user role is normal user, navigate they to Employer Registration Page and make they fill the Employer Profile with data is [Employer Profile Info](#mock-data)
     2. If user already logged with role is employer then remove the Become Employer button on navbar and navigate they to Employer Dashboard page
   - Component path:
     - /src/components/Navbar.tsx
     - /src/app/employer/dashboard/page.tsx

## Mock Data

1. Employer profile info
   
```
    {
      id: {
        type: DataTypes.UUID,
        defaultValue: DataTypes.UUIDV4,
        primaryKey: true,
      },
      user_id: {
        type: DataTypes.UUID,
        allowNull: false,
        unique: true,
      },
      company_name: {
        type: DataTypes.STRING,
        allowNull: true,
      },
      company_website: {
        type: DataTypes.STRING,
        allowNull: true,
      },
      company_logo: {
        type: DataTypes.STRING,
        allowNull: true,
      },
      company_description: {
        type: DataTypes.TEXT,
        allowNull: true,
      },
      industry: {
        type: DataTypes.STRING,
        allowNull: true,
      },
      company_size: {
        type: DataTypes.STRING,
        allowNull: true,
        validate: {
          isIn: [["1-10", "11-50", "51-200", "201-500", "501-1000", "1000+"]],
        },
      },
      is_verified: {
        type: DataTypes.BOOLEAN,
        defaultValue: false,
      },
    }
```